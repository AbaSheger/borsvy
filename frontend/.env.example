# Frontend local development example.
# In dev, the Vite proxy already forwards /api to http://localhost:8080.
# Leave VITE_API_URL empty for same-origin API calls.

VITE_API_URL=
VITE_GOOGLE_CLIENT_ID=
